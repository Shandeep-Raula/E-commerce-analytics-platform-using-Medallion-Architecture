CREATE OR REPLACE TABLE ECOMMERCE.LANDING.RAW_ORDER (
    ORDER_LINE_ID VARCHAR,
    ORDER_ID VARCHAR,
    ORDER_DATE DATE,
    CUSTOMER_ID VARCHAR,
    PRODUCT_ID VARCHAR,
    SELLER_ID VARCHAR,
    LOCATION_ID VARCHAR,
    DELIVERY_PERSON_ID VARCHAR,
    PAYMENT_ID VARCHAR,
    CAMPAIGN_ID VARCHAR,
    FULFILLMENT_ID VARCHAR,
    QUANTITY NUMBER(10,0),
    UNIT_PRICE NUMBER(10,2),
    DISCOUNT_PERCENTAGE NUMBER(5,2),
    TAX_PERCENTAGE NUMBER(5,2),
    SHIPPING_FEE NUMBER(10,2),
    EXPECTED_DELIVERY_DATE DATE,
    ACTUAL_DELIVERY_DATE DATE,
    DELIVERY_RATING NUMBER(3,2),
    ORDER_STATUS VARCHAR,
    RETURN_FLAG BOOLEAN,
    REFUND_AMOUNT NUMBER(10,2),
    RETURN_DATE DATE,
    SELLER_RATING NUMBER(3,2),
    CUSTOMER_RATING NUMBER(3,2),
    REVIEW_TEXT VARCHAR,
    SENTIMENT_SCORE NUMBER(5,2),
    DELIVERY_DELAY_DAYS NUMBER(5,0),
    DELIVERY_SLA_DAYS NUMBER(5,0),
    STATE VARCHAR,
    CITY VARCHAR,
    POSTAL_CODE VARCHAR,
    FIRST_NAME VARCHAR,
    LAST_NAME VARCHAR,
    EMAIL VARCHAR,
    PHONE_NUMBER VARCHAR,
    LOCATION_ID_CUSTOMER VARCHAR,
    PRODUCT_NAME VARCHAR,
    BRAND VARCHAR,
    CATEGORY_PRODUCT VARCHAR,
    SUB_CATEGORY VARCHAR,
    LIST_PRICE NUMBER(10,2),
    DELIVERY_PERSON_NAME VARCHAR,
    PHONE_NUMBER_DELIVERY_PERSON VARCHAR,
    VEHICLE_TYPE VARCHAR,
    LOCATION_ID_DELIVERY_PERSON VARCHAR,
    PAYMENT_METHOD VARCHAR,
    PAYMENT_PROVIDER VARCHAR,
    PAYMENT_DETAILS VARCHAR,
    SERVICE_LEVEL VARCHAR,
    SELLER_NAME VARCHAR,
    EMAIL_SELLER VARCHAR,
    PHONE_NUMBER_SELLER VARCHAR,
    _LOADED_AT TIMESTAMP_NTZ DEFAULT CURRENT_TIMESTAMP()
)